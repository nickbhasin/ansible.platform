---
driver:
  name: default

platforms:
  - name: localhost

ansible:
  executor:
    args:
      ansible_playbook:
        - --inventory=${MOLECULE_SCENARIO_DIRECTORY}/../inventory.yml

provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
    verify: verify.yml
    cleanup: cleanup.yml
  config_options:
    defaults:
      collections_path: "${ANSIBLE_COLLECTIONS_PATH:-${MOLECULE_SCENARIO_DIRECTORY}/../../../../../../}"

scenario:
  test_sequence:
    - converge
    - verify
    - cleanup
...
